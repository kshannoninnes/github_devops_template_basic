{
  "name": "devops_git_setup",
  "version": "1.0.0",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/devops-git-setup.cjs --banner:js='#!/usr/bin/env node'",
    "start": "node dist/devops-git-setup.cjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "simple-git": "^3.30.0"
  }
}
